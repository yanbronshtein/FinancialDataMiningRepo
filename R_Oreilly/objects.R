#Object is a name that you can use to call up stored data. For example,
# You can use to call up stored data. For example, you can save data into an 
# Object like a or b. Wherever R encounters the object, 
#it will replace it with the data saved inside, like
#so:
a <- 1
a
a + 2
